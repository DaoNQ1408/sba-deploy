spring:
  application:
    name: workshop-be-sql

  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=sba_workshop;encrypt=true;trustServerCertificate=true
    username: sa
    password: 12345
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.SQLServerDialect

server:
  port: 8080
